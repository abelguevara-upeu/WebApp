export const API_CONFIG = {
  // Para desarrollo local
  // baseUrl: 'http://localhost:9090/api'

  // Para servidor intranet con proxy Apache (RECOMENDADO)
  baseUrl: '/api'
  
  // Para acceso directo al backend (alternativo)
  // baseUrl: 'http://appsrv.abel-company.intranet:9090/api'
};